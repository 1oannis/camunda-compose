services:
  traefik:
    image: traefik:${TRAEFIK_VERSION}
    container_name: traefik
    deploy:
      resources:
        limits:
          memory: 600M
          cpus: '0.5'
        reservations:
          memory: 300M
          cpus: '0.25'
    restart: always
    command:
      # Disable unsecured dashboard/API in production, port 8090 is used for it
      - "--api.dashboard=true"
      - "--api.insecure=true"
      # Logging
      - "--log.level=WARN"
      - "--log.maxAge=1"
      - "--log.compress=true"
      - "--accesslog=true"
      # Entrypoints
      - "--entrypoints.web.address=:80"
      - "--entrypoints.websecure.address=:443"
      # Docker provider
      - "--providers.docker=true"
      - "--providers.docker.exposedbydefault=false"
      # Let's Encrypt configuration
      - "--certificatesresolvers.letsencrypt.acme.tlschallenge=true"
      - "--certificatesresolvers.letsencrypt.acme.email=${ACME_EMAIL}"
      - "--certificatesresolvers.letsencrypt.acme.storage=/letsencrypt/acme.json"
    ports:
      - "80:80"
      - "443:443"
      - "8080:8080" # Traefik dashboard (development only)
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - type: bind
        source: /opt/volumes/letsencrypt
        target: /letsencrypt
    networks:
      - camunda-platform
      - identity-network
      - web-modeler

networks:
  camunda-platform:
  identity-network:
  web-modeler: