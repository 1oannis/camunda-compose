# Host-specific Camunda 8 Platform Configuration
# Copy this file to runtime_local_HOSTNAME.env and adjust the values for your server

# Network Configuration
HOST=camunda.example.com
KEYCLOAK_HOST=camunda.example.com

# Security Overrides
# Override these with your secure passwords
POSTGRES_PASSWORD=SecurePostgresPassword
KEYCLOAK_ADMIN_PASSWORD=SecureKeycloakAdminPassword
ZEEBE_CLIENT_SECRET=SecureZeebeClientSecret
WEB_MODELER_DB_PASSWORD=SecureWebModelerPassword

# Memory settings - adjust based on server capacity
ZEEBE_MEMORY=4G
ELASTICSEARCH_MEMORY=4G
OPERATE_MEMORY=2G
TASKLIST_MEMORY=2G
OPTIMIZE_MEMORY=2G
WEB_MODELER_MEMORY=2G

# File system paths - adjust if necessary
DATA_PATH=/opt/camunda-compose/docker.camunda-8

# Network ports - adjust if ports are already in use on host
ZEEBE_PORT=26500
OPERATE_PORT=8081
TASKLIST_PORT=8082
OPTIMIZE_PORT=8083
IDENTITY_PORT=8084
CONNECTOR_PORT=8085
KEYCLOAK_PORT=18080
ELASTICSEARCH_PORT=9200
WEB_MODELER_PORT=8070
WEB_MODELER_WEBSOCKETS_PORT=8060
